<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *>
  @type forward
  <server>
    host otel-collector
    port 24225
  </server>
  <buffer>
    @type file
    path /fluentd/log/buffer
    flush_mode interval
    flush_interval 1s
    retry_max_times 3
  </buffer>
</match>

